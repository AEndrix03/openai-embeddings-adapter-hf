# Task completion checklist
- Run tests with `python -m pytest -q`.
- If relevant, update docs/config examples (`README.md`, compose, env examples).
- Keep settings/env compatibility backward compatible where possible.
- Summarize changed files and mention any limitations (e.g., missing local binaries).